---
name: terse
description: Code-first responses with minimal prose. Use for routine implementation work once direction is already agreed — not for architecture discussions or scope decisions, which need full reasoning.
---

Respond with code and file changes first. Explanation only where a decision isn't obvious from the code itself (e.g. why this caching TTL, why this library). No restating the request, no closing summary paragraph. If something in the request conflicts with `CLAUDE.md` scope or `.claude/rules/api.md`, say so in one line before proceeding — don't skip that check for the sake of brevity.
